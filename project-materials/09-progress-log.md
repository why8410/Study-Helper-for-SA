# 진행 로그

이 문서는 `Study Helper for SA Tablet` 프로젝트에서
실제로 어떤 작업을 추가했는지 날짜 기준으로 누적 정리하는 로그입니다.

## 2026-03-16

### 1차 시작

- 갤럭시 태블릿용 별도 프로젝트 폴더 생성
- 태블릿 우선 UI 초안 구성
- 카메라, 사진 업로드, 음성 입력, 음성 읽기 연결
- 브라우저 OCR과 힌트형 코칭 로직 추가
- ChatGPT용 질문 복사 기능 추가
- PWA용 manifest, service worker 추가

### 실행 보조

- `Start-Study-Helper-for-SA-Tablet.command`
- `Stop-Study-Helper-for-SA-Tablet.command`
- 정적 앱 검증 스크립트와 테스트 추가

### 문서화

- `project-materials/` 폴더 생성
- 기획서, UX 문서, 기술 설계서, 초보자용 개발 설명서 작성

### 사용성 보강

- `공유하기` 버튼 추가
- 최근 코칭 기록 저장과 다시 불러오기 추가
- 최근 기록 직접 지우기 추가
- OCR 전에 이미지 대비를 보정하는 전처리 추가
- `촬영 가이드` 카드로 OCR 상태를 쉬운 말로 안내하도록 보강
- 화면 표시 서비스명을 `Study Helper for SA` 중심으로 단순화

### 배포 준비 보강

- 정적 배포용 릴리스 폴더 생성 스크립트 추가
- 더블클릭용 릴리스 생성 실행 파일 추가
- 갤럭시 태블릿 실사용 테스트 가이드 문서 추가
- 임시 HTTPS 터널로 실제 태블릿 테스트 주소 생성 성공
- 임시 링크는 Mac 전원이 켜져 있어야 한다는 설명 문서 추가
- Netlify 업로드용 릴리스 zip 파일 준비
- Netlify 무료 플랜 비용/안전 메모 문서 추가
- 실제 사용 기준 팁을 화면과 문서에 추가
- Netlify 배포 후 ChatGPT 로그인 필요 여부를 문서에 명확히 추가
- 화면에서 `기본 사용은 GPT 로그인 불필요` 문구가 바로 보이도록 보강
- API 비용 없이 GPT를 활용하는 현실적 선택지 문서 추가
- ChatGPT 복사와 공유하기 중심으로 보조 흐름 단순화
- Netlify 업로드 성공, 기본 사이트 주소 확인
- 릴리스 생성 시 최신 zip도 자동으로 다시 만들도록 보강
- Netlify 재배포 후 최신 UI 반영 확인 완료
- PC 화면 재촬영 시 OCR이 약해질 수 있다는 테스트 메모 추가
- 실사용 피드백 기준으로 OCR 2차 보정 읽기와 음성 버튼 상태 표시 보강
- 서비스 제목을 `Study Helper for SA` 하나로 정리하고 `For Tablet` 을 보조 문구로 축소
- 상단 키워드를 설명형 메타 정보처럼 더 작고 가볍게 조정
- `질문하기` 입력 영역으로 용어를 단순화하고 예시 텍스트와 실제 입력 텍스트 색을 구분
- 힌트 카드와 마지막 단계 카드 안에 `자동 읽기` 버튼을 배치
- 음성 입력으로 말한 문제 번호가 입력칸에도 자동 반영되도록 보강
- `촬영 가이드` 카드를 제거하고 마지막 단계에서 정답과 풀이를 더 직접적으로 보여주도록 보강

### 검증

- `python3 scripts/verify_static_app.py` 통과
- `python3 -m unittest discover -s tests` 통과
- 로컬 미리보기 주소 `http://127.0.0.1:8010` 확인

## 2026-03-26

### 촬영 UX 보강

- 메인 카드 안 라이브 카메라 대신 `사진 찍기` 버튼을 두고, 누르면 별도 `전체화면 촬영` 레이어가 열리도록 구조 변경
- 촬영은 전체화면에서 진행하고, 찍은 뒤에는 메인 화면에 결과 미리보기만 남도록 정리
- `다시 찍기` 흐름과 촬영 닫기 흐름을 추가해서 태블릿/폰에서 더 편하게 사용하도록 보강
- 서비스 워커 캐시 버전을 올려서 배포 후 예전 화면이 남을 가능성을 줄이도록 보강
- 관련 문서와 테스트 가이드를 전체화면 촬영 흐름 기준으로 업데이트

## 2026-04-01

### 현재 상태 재점검

- `python3 scripts/verify_static_app.py` 재실행 통과
- `python3 -m unittest discover -s tests` 재실행 통과
- 릴리스 생성 스크립트를 다시 실행해 최신 `release/study-helper-for-sa-tablet-web/` 와 zip 파일을 갱신

### 테스트 준비 보강

- 실제 갤럭시 태블릿 테스트 결과를 바로 적을 수 있는 `12-tablet-test-report-template.md` 추가
- 자료 안내 문서에 새 테스트 기록 템플릿을 연결

## 2026-04-06

### 배포 준비 정리

- 릴리스 번들 생성 스크립트가 `DEPLOY-NOTES.txt` 뿐 아니라 `TABLET-TEST-CHECKLIST.txt` 도 함께 넣도록 보강
- 릴리스 번들 테스트를 업데이트해서 새 체크리스트 파일과 주요 안내 문구를 함께 검증

### 문서 상태 정리

- 배포 가이드의 현재 상태 설명을 실제 진행 로그와 맞게 정리
- 루트 `README.md` 에 릴리스 폴더 안 생성 안내 파일을 명시

## 2026-04-08

### GitHub 업로드

- 프로젝트 폴더를 Git 저장소로 초기화하고 `main` 브랜치 기준 첫 커밋 생성
- GitHub 계정 `why8410` 에 `Study-Helper-for-SA` 저장소 생성 후 원격 푸시 완료

### GitHub Pages 준비

- GitHub Pages 프로젝트 경로에서도 깨지지 않도록 정적 자산 경로를 상대 경로 기준으로 정리
- `manifest.webmanifest` 의 `start_url`, `scope`, 아이콘 경로를 프로젝트 배포에 맞게 조정
- 서비스 워커 등록 경로와 오프라인 fallback 경로를 프로젝트 하위 경로에 맞게 보강
- `.github/workflows/deploy-pages.yml` 추가로 GitHub Actions 기반 Pages 배포 준비

## 2026-04-09

### 실제 태블릿 테스트 반영

- 카메라 촬영 시 더 높은 해상도를 우선 시도하도록 보강하고, 가능할 때는 `ImageCapture` 기반 고화질 촬영을 먼저 사용하도록 개선
- OCR 전처리와 재시도 단계를 보강해서 약한 사진일 때 3차 선명 읽기까지 시도하도록 조정
- `읽은 문제와 코칭 결과` 영역을 상단 상태 버튼 + 펼침형 상세 패널 구조로 변경
- `최근 코칭 기록` 도 기본 목록은 간단히 보여주고, 필요한 경우에만 펼쳐서 상세를 보는 구조로 변경
- 마지막 단계 카드를 힌트 카드와 다른 색/분위기로 다시 디자인하고, 힌트 3단계를 모두 열면 정답과 풀이 버튼이 활성화되도록 흐름 보강
- `ChatGPT 보조` 상단 중복 설명 문구 제거
